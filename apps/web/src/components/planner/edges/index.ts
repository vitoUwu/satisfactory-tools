/** React Flow edge type registry. */

import type { EdgeTypes } from "@xyflow/react";

import { BeltPipeEdge } from "./BeltPipeEdge";

export const edgeTypes: EdgeTypes = {
  beltPipe: BeltPipeEdge,
};
